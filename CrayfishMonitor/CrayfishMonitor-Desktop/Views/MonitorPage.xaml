<Page
    x:Class="CrayfishMonitor_Desktop.Views.MonitorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CrayfishMonitor_Desktop.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="20">
        <StackPanel Orientation="Vertical" >
            <TextBlock Text="データ計測" FontSize="32"/>
            <StackPanel Orientation="Horizontal" Margin="0 20">
                <TextBlock Text="接続先" Margin="10 5"/>
                <ComboBox Margin="10 0"
                          ItemsSource="{x:Bind ViewModel.DeviceName}"
                          SelectedIndex="{x:Bind ViewModel.SelectedDeviceIndex.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                          Width="300"/>
                <ToggleButton Content="計測" IsEnabled="true"
                              Margin="10 0"
                              Width="80"
                              Command="{x:Bind ViewModel.MeasureButton}"
                              IsChecked="{x:Bind ViewModel.MeasureButtonState.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
